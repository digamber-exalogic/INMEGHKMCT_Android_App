<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context=".Activities.AdminMenuEmailCompose">

    <EditText
        android:id="@+id/etEmailTo"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:background="@drawable/rectangular_border1"
        android:drawableLeft="@drawable/ic_name"
        android:drawableStart="@drawable/ic_name"
        android:hint="To..."
        android:inputType="textEmailAddress"
        android:textStyle="italic" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginRight="10dp"
        android:background="@drawable/rectangle_border">

        <android.support.v7.widget.RecyclerView
            android:id="@+id/emailrv"
            android:layout_width="match_parent"
            android:layout_height="130dp">

        </android.support.v7.widget.RecyclerView>

        <CheckBox
            android:id="@+id/chbEmailAll"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:layout_alignParentRight="true"
            android:layout_alignParentTop="true"
            android:background="@animator/shape_round"
            android:buttonTint="#fff"
            android:text="All"
            android:textColor="#fff"
            android:textStyle="bold" />

    </RelativeLayout>


    <EditText
        android:id="@+id/etEmailSubject"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:background="@drawable/rectangular_border1"
        android:drawableLeft="@drawable/ic_email_subject"
        android:drawableStart="@drawable/ic_email_subject"
        android:hint=" Subject..."
        android:inputType="textEmailAddress"
        android:textStyle="italic"/>

    <EditText
        android:id="@+id/etEmailBody"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="top"
        android:layout_marginBottom="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginStart="10dp"
        android:background="@animator/selector"
        android:drawableLeft="@drawable/ic_email_create_msg"
        android:drawableStart="@drawable/ic_email_create_msg"
        android:gravity="top"
        android:hint="  Compose email..."
        android:inputType="textEmailAddress"
        android:textStyle="italic" />


</LinearLayout>